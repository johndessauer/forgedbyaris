export interface InitOptions {
    /**
     * Target the local test API (http://localhost:3010) instead of production.
     * Primarily for Memberstack's own integration tests.
     */
    test?: boolean;
    /**
     * Override the admin API base URL entirely (e.g. a staging endpoint).
     * Takes precedence over `test`. A trailing slash is optional.
     */
    baseUrl?: string;
}
export declare const init: (secretKey: string, options?: InitOptions) => {
    members: import("./methods/members").default;
    dataTables: import("./methods/dataTables").default;
    verifySignature: ({ signatureHeader, secret, payload, tolerance }: {
        signatureHeader: string;
        secret: string;
        payload: object;
        tolerance?: number;
    }) => boolean;
    verifyWebhookSignature: ({ headers, secret, payload, tolerance }: {
        headers: object;
        secret: string;
        payload: object;
        tolerance?: number;
    }) => boolean;
    verifyToken: ({ token, audience }: {
        token: string;
        audience?: string;
    }) => Promise<Pick<import("jose").JWTPayload, "iat" | "exp"> & {
        id: string;
        type: "member";
    }>;
};
declare const _default: {
    init: (secretKey: string, options?: InitOptions) => {
        members: import("./methods/members").default;
        dataTables: import("./methods/dataTables").default;
        verifySignature: ({ signatureHeader, secret, payload, tolerance }: {
            signatureHeader: string;
            secret: string;
            payload: object;
            tolerance?: number;
        }) => boolean;
        verifyWebhookSignature: ({ headers, secret, payload, tolerance }: {
            headers: object;
            secret: string;
            payload: object;
            tolerance?: number;
        }) => boolean;
        verifyToken: ({ token, audience }: {
            token: string;
            audience?: string;
        }) => Promise<Pick<import("jose").JWTPayload, "iat" | "exp"> & {
            id: string;
            type: "member";
        }>;
    };
};
export default _default;
